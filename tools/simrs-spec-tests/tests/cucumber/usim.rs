#![allow(missing_docs)]
//! Step definitions for `usim.feature` -- USIM application.
//!
//! Crates under test: `simrs-usim`, `simrs-milenage`.

use crate::world::SpecWorld;

// Step definitions will be added incrementally.
const _: fn(&SpecWorld) = |_w| {};
