#![allow(missing_docs)]
//! Step definitions for `transport-tcp.feature` -- TCP transport.
//!
//! Crate under test: `simrs-transport-tcp`.

use crate::world::SpecWorld;

// Step definitions will be added incrementally.
const _: fn(&SpecWorld) = |_w| {};
