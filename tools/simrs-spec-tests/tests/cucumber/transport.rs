#![allow(missing_docs)]
//! Step definitions for `transport.feature` -- Transport abstraction.
//!
//! Crate under test: `simrs-transport`.

use crate::world::SpecWorld;

// Step definitions will be added incrementally.
const _: fn(&SpecWorld) = |_w| {};
