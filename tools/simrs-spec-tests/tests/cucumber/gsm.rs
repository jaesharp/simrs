#![allow(missing_docs)]
//! Step definitions for `gsm.feature` -- GSM SIM application.
//!
//! Crates under test: `simrs-gsm`, `simrs-comp128`.

use crate::world::SpecWorld;

// Step definitions will be added incrementally.
const _: fn(&SpecWorld) = |_w| {};
