#![allow(missing_docs)]
//! Step definitions for `fs.feature` -- SIM filesystem operations.
//!
//! Crate under test: `simrs-fs`.

use crate::world::SpecWorld;

// Step definitions will be added incrementally.
const _: fn(&SpecWorld) = |_w| {};
