[package]
name        = "simrs-jccompile"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "JVA compiler core: IR, type system, optimizer, and codegen for smartcard applets"

[features]
default = ["std"]
std     = []

[dependencies]
simrs-jcvm-opcodes = { workspace = true }

[dev-dependencies]
simrs-jcvm = { workspace = true }

[lints]
workspace = true
