[package]
name        = "simrs-hle"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "HLE SIM peripheral for QEMU -- C-ABI exports for APDU injection -- std"

[lib]
# cdylib deferred -- C-ABI exports require unsafe (raw pointer dereference)
crate-type = ["rlib"]

[features]
default = []

[dependencies]
simrs-sim      = { workspace = true, features = ["usim", "gsm"] }
simrs-fs       = { workspace = true }
simrs-milenage = { workspace = true }
simrs-tuak     = { workspace = true }
simrs-gsm      = { workspace = true }
simrs-usim     = { workspace = true }
simrs-profile  = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
