[project]
name = "simrs"
version = "0.1.0"
description = "Python bindings for the SimRS smart card simulator"
license = "GPL-2.0-or-later"
requires-python = ">=3.10"
authors = [
    { name = "J Lynn", email = "j@jaesharp.com" },
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[dependency-groups]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
