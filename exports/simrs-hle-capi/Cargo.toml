[package]
name        = "simrs-hle-capi"
version     = "0.1.0"
edition     = "2021"
license     = "LGPL-2.0-or-later"
description = "C-ABI wrapper for simrs-hle -- shared (.so) and static (.a) library with cbindgen header"
publish     = false

[lib]
crate-type = ["cdylib"]

[dependencies]
simrs-hle     = { path = "../../crates/simrs-hle" }
simrs-profile = { path = "../../crates/simrs-profile" }

[build-dependencies]
cbindgen = "0.29"

# Empty workspace table: prevents cargo from treating this as part of the
# parent simrs workspace (which enforces `unsafe_code = "forbid"`).
[workspace]

[profile.release]
lto = "fat"
codegen-units = 1
strip = "symbols"
